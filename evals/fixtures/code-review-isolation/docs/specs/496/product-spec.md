# Product Spec

## Goal

The public `greeting()` behavior must change from returning `"before"` to returning exactly `"after"`.

## Acceptance criteria

- Calling `greeting()` returns exactly `"after"`.
- The exported function name and call shape remain unchanged.
- No additional product behavior is required for this fixture.
