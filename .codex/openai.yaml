interface:
  display_name: "Harness Engineering"
  short_description: "Requirements and use case orchestration"
  default_prompt: "Use $setup when repository initialization is needed, then use $spec-me for product and architecture specifications."

policy:
  allow_implicit_invocation: true
