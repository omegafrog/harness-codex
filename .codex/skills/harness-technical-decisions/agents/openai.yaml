interface:
  display_name: "Harness Technical Decisions"
  short_description: "선택된 work item의 구현 차단 기술 결정을 확정합니다"
  default_prompt: "$harness-technical-decisions로 선택된 UC 또는 maintenance work item의 기술 결정을 확정해줘."

policy:
  allow_implicit_invocation: true
