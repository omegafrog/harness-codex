interface:
  display_name: "DDD 후보 설계"
  short_description: "이벤트 스토밍으로 UC별 후보 DDD 설계를 만듭니다"
  default_prompt: "$harness-ddd-design으로 대상 UC의 후보 DDD 설계를 진행해줘."

policy:
  allow_implicit_invocation: true
