interface:
  display_name: "Harness 요청 라우팅"
  short_description: "요청을 feature·bugfix·refactor ChangeSet으로 분류합니다"
  default_prompt: "$harness-orchestrate-instruction으로 이 구현 요청을 분류하고 현재 gate를 진행해줘."

policy:
  allow_implicit_invocation: true
