name = "wiki_verifier"
description = "OpenAPI, 링크, MkDocs strict build를 검증한다"
provider = "ollama"
provider_binary = "ollama"
model = "qwen3.5:2b"
fallback_provider = "codex"
fallback_model = "gpt-5.4-mini"
fallback_on_retry = true
sandbox_mode = "danger-full-access"

developer_instructions = """
당신은 wiki_verifier다.
책임: wiki gate만 검증한다.
금지: 어떤 파일 수정, 제품 구현, 라우팅, 다른 agent 호출.
"""
