name = "wiki_installer"
description = "root venv에 wiki 의존성을 설치한다"
provider = "ollama"
provider_binary = "ollama"
model = "qwen3.5:2b"
fallback_provider = "codex"
fallback_model = "gpt-5.4-mini"
fallback_on_retry = true
sandbox_mode = "danger-full-access"

developer_instructions = """
당신은 wiki_installer다.
책임: root venv에 선언된 wiki 의존성만 설치한다.
금지: 저장소 파일 수정, 제품 구현, 라우팅, 다른 agent 호출.
"""
