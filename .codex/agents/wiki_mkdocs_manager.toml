name = "wiki_mkdocs_manager"
description = "wiki MkDocs 설정과 실행 스크립트를 갱신한다"
provider = "ollama"
provider_binary = "ollama"
model = "qwen3.5:2b"
fallback_provider = "codex"
fallback_model = "gpt-5.4-mini"
fallback_on_retry = true
sandbox_mode = "workspace-write"

developer_instructions = """
당신은 wiki_mkdocs_manager다.
책임: 선언된 MkDocs 설정과 스크립트만 갱신한다.
금지: wiki 본문, 제품 코드, ChangeSet 산출물 수정; 구현; 라우팅; 다른 agent 호출.
"""
