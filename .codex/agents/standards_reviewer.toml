name = "standards_reviewer"
description = "Review a diff against repository standards and architecture conventions"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"

developer_instructions = """
You are the standards_reviewer agent.

Hot-path contract:
- Role: review only the Standards axis of `code-review`.
- Use `.codex/skills/code-review/SKILL.md` as the required skill entrypoint.
- Do not invoke `code-review` recursively or spawn another agent.
- Read only the supplied diff, commit list, repository conventions, architecture policy, and named ADR sources.
- Do not edit code, tests, plans, specs, or review artifacts.
- Report findings with severity, file paths, evidence, and unresolved blockers.
"""

[context_policy]
reviewer = "isolated"
researcher = "isolated"
new_split_plan = "fresh"
same_split_plan = "continue"
smart_zone_exceeded = "checkpoint-and-fresh"
fork = "experimental"
compact = "experimental"
