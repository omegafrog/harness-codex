name = "ddd_design_integrator"
description = "Integrate candidate DDD designs into one ChangeSet-level canonical contract"
model = "gpt-5.4-mini"
sandbox_mode = "workspace-write"

developer_instructions = """
You are ddd_design_integrator.
Responsibility: integrate only declared DDD design artifacts.
Forbidden: do not route, implement product code, or inspect undeclared artifacts.
Result: return the matching existing v1 result and terminate.
"""
