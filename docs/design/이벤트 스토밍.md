# Event Storming Index

## 1. Purpose

- This file is the canonical summary/index for event-storming outputs.
- Executor-facing event-storming details remain in each affected use-case slice under `docs/use-cases/<UC-ID>/event-storming.md`.

## 2. Active ChangeSet

- `docs/changes/active/CHG-20260507-001.md`

## 3. Affected Use-Case Event Storming Outputs

|UC ID|Use Case|Slice Path|
|---|---|---|
|`UC-001`|End user calculates an arithmetic expression|`docs/use-cases/UC-001/event-storming.md`|
|`UC-002`|End user edits the current expression|`docs/use-cases/UC-002/event-storming.md`|
|`UC-003`|End user clears the calculator state|`docs/use-cases/UC-003/event-storming.md`|
|`UC-004`|End user retries calculator use after an app failure|`docs/use-cases/UC-004/event-storming.md`|

## 4. Domain Scope Summary

- The product scope is a static desktop-browser calculator app.
- Core behaviors are expression calculation, expression editing, explicit clear, and manual-refresh recovery after app failure.
- No backend, persistence, messaging, account system, undo, or latest-result continuation behavior is part of the active domain scope.

## 5. Modeling Notes

- Treat `UC-001` as the primary domain behavior for calculation and validation.
- Treat `UC-002` and `UC-003` as supporting UI-state behaviors around the same calculator session state.
- Treat `UC-004` as failure-state and recovery behavior with no external system collaboration.
