# 사용자 흐름

사용자는 수식을 인자로 전달해 계산합니다.

```bash
./venv/bin/python3 -m harness_codex.calculator.cli "2 + 3"
```

정상 수식은 계산 결과 한 줄을 표준 출력으로 내보내고 종료 코드 `0`으로 끝납니다. 지원 범위는 `+`, `-`, `*`, `/`, 괄호와 정수·소수 리터럴입니다.

잘못된 수식 또는 0으로 나누기는 오류를 표준 오류로 내보내며, 0이 아닌 종료 코드로 끝납니다.
